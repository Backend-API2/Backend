spring.application.name=Backend

# Base de datos H2 para desarrollo local
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# H2 Console (para ver la base de datos)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# JPA/Hibernate Configuration
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Disable data.sql execution for H2
spring.sql.init.mode=never

# Logging configuration
logging.file.name=logs/application.log
logging.level.backend_api.Backend=INFO
logging.level.org.springframework.web=INFO
logging.pattern.file=%d{yyyy-MM-dd HH:mm:ss} - %msg%n

# JWT Configuration
jwt.secret=paymentModuleSecretKey12345678901234567890123456789012345
jwt.expiration=86400000
jwt.issuer=payment-module

# SpringDoc OpenAPI Configuration
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operationsSorter=method
springdoc.swagger-ui.tagsSorter=alpha
springdoc.swagger-ui.tryItOutEnabled=true
springdoc.swagger-ui.filter=true
springdoc.swagger-ui.display-request-duration=true
springdoc.swagger-ui.show-extensions=true
springdoc.swagger-ui.show-common-extensions=true

# Security
server.servlet.session.cookie.secure=false
server.servlet.session.cookie.http-only=true

# Server configuration
server.address=0.0.0.0
server.port=8080

# Actuator - Configuración mínima para métricas locales
management.endpoints.web.exposure.include=health,info,metrics,prometheus
management.endpoint.health.show-details=when-authorized
management.endpoint.info.enabled=true
management.endpoint.metrics.enabled=true
management.endpoint.prometheus.enabled=true

# Micrometer Prometheus configuration
management.metrics.export.prometheus.enabled=true
management.metrics.export.prometheus.step=60s
management.metrics.tags.application=${spring.application.name}
management.metrics.tags.environment=local

# Actuator info
info.app.name=Backend Payment API
info.app.description=API de pagos y facturación
info.app.version=@project.version@
info.app.java.version=@java.version@

# Bean override configuration
spring.main.allow-bean-definition-overriding=true

# Logging
logging.level.backend_api.Backend.Auth=INFO
logging.level.org.springframework.security=WARN
logging.level.backend_api.Backend.messaging=DEBUG

# CORE HUB API Configuration (valores por defecto para desarrollo)
core.hub.api.url=${CORE_HUB_URL:https://nonprodapi.uade-corehub.com}
core.hub.api.key=${CORE_HUB_API_KEY:ch_41b214dbb9064053a03f74e68b2385c6}
core.hub.team.name=${CORE_HUB_TEAM:payments}
core.hub.webhook.url=${CORE_HUB_WEBHOOK_URL:https://a996f7d3f540.ngrok-free.app/api/core/webhook/payment-events}
core.hub.user.webhook.url=${CORE_HUB_USER_WEBHOOK_URL:https://a996f7d3f540.ngrok-free.app/api/core/webhook/user-events}
